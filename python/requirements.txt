# SXWer AI ChatBot — Python microservice dependencies
#
# Install: pip install -r requirements.txt
# Start:   uvicorn app:app --host 127.0.0.1 --port 8000
#
# Optional heavy dependencies are commented out with install instructions.
# The service degrades gracefully when they are absent.

# ---------------------------------------------------------------------------
# Core API framework
# ---------------------------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9      # required for file uploads (voice/transcribe)

# ---------------------------------------------------------------------------
# NLP & Sentiment Analysis
# ---------------------------------------------------------------------------
# transformers and torch add ~1–2 GB but enable on-device sentiment analysis.
# The service runs without them using keyword-based fallback.
#
# To install (CPU-only, all platforms):
#   pip install transformers torch --index-url https://download.pytorch.org/whl/cpu
#
# transformers>=4.40.0
# torch>=2.0.0

# ---------------------------------------------------------------------------
# Safety Classifier
# ---------------------------------------------------------------------------
scikit-learn>=1.4.0           # TF-IDF + logistic regression safety model

# ---------------------------------------------------------------------------
# Voice Pipeline — STT (Vosk)
# ---------------------------------------------------------------------------
# vosk adds ~30 MB + requires a separate model download.
# Model download: https://alphacephei.com/vosk/models
# Set VOSK_MODEL_PATH in .env after extracting the model.
#
# vosk>=0.3.45

# ---------------------------------------------------------------------------
# Voice Pipeline — TTS (pyttsx3)
# ---------------------------------------------------------------------------
# pyttsx3 uses system-installed voices and works offline on all platforms.
#
# pyttsx3>=2.90

# ---------------------------------------------------------------------------
# Resource Manager — async HTTP client
# ---------------------------------------------------------------------------
httpx>=0.27.0                 # async URL validation for resources.json

# ---------------------------------------------------------------------------
# Optional: Higher-quality TTS via Mimic 3
# ---------------------------------------------------------------------------
# Run Mimic 3 as a Docker container (no pip install needed):
#   docker run -d -p 59125:59125 mycroftai/mimic3
# Then set TTS_ENGINE=mimic3 and MIMIC3_URL=http://localhost:59125 in .env.
